# ============================================================================
# Alation Configuration (Required for MCP Server)
# ============================================================================

# Alation instance URL (no trailing slash)
ALATION_BASE_URL=https://your-company.alation.com

# Alation API Refresh Token
# Generate in Alation: Account Settings → Authentication → Refresh Tokens
# The bot will automatically exchange this for an API Access Token on startup.
ALATION_API_TOKEN=your_refresh_token_here

# REQUIRED: Your numeric Alation user ID
# Find it by logging into Alation → click your profile → the URL shows /user/<ID>/
# Example: if your profile URL is https://alation.company.com/user/42/ then set 42
ALATION_USER_ID=

# ============================================================================
# Slack Configuration (Existing)
# ============================================================================

# Slack Bot User OAuth Token (starts with xoxb-)
SLACK_BOT_TOKEN=xoxb-your-bot-token-here

# Slack App-Level Token for Socket Mode (starts with xapp-)
SLACK_APP_TOKEN=xapp-your-app-token-here

# Slack Signing Secret
SLACK_SIGNING_SECRET=your-signing-secret-here

# ============================================================================
# AWS Configuration (Existing - for Bedrock)
# ============================================================================

# AWS Access Key ID
AWS_ACCESS_KEY_ID=your-aws-access-key-id

# AWS Secret Access Key
AWS_SECRET_ACCESS_KEY=your-aws-secret-access-key

# AWS Region
AWS_DEFAULT_REGION=us-west-2


# ============================================================================
# Usage Instructions
# ============================================================================
#
# 1. Copy this file to .env:
#    cp .env.example .env
#
# 2. Fill in your actual credentials
#
# 3. Never commit .env to git (it's in .gitignore)
#
# 4. For Alation setup:
#    - Log into Alation → Account Settings → Authentication → Refresh Tokens
#    - Generate a Refresh Token and paste it as ALATION_API_TOKEN above
#    - Find your numeric user ID from your Alation profile URL (/user/<ID>/)
#    - The bot automatically exchanges the Refresh Token for an API Access
#      Token on startup
#
# 5. Start the bot:
#    python -m app.socket_mode
#
# ============================================================================
